import { Repeat } from '@material-ui/icons'

import { iconVariantBuilder } from './base'

export const [
  RepeatIcon,
  RepeatBadgeIcon,
  RepeatButtonIcon,
  RepeatButtonBadgeIcon,
] = iconVariantBuilder( Repeat )
