import { Timer } from '@material-ui/icons'

import { iconVariantBuilder } from './base'

export const [
  TimerIcon,
  TimerBadgeIcon,
  TimerButtonIcon,
  TimerButtonBadgeIcon,
] = iconVariantBuilder( Timer )
