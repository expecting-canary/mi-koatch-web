import { operation } from './model/operation';

export const handlers = {
  SELECT_SESSION: operation.select.session,
  SELECT_EXERCICE: operation.select.exercice,
  SELECT_SERIE: operation.select.serie
};
