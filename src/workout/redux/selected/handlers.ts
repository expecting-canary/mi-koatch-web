import { Selected } from '../../models';

export const handlers = {
  SELECT_SESSION: Selected.action.select.session,
  SELECT_EXERCICE: Selected.action.select.exercice,
  SELECT_SERIE: Selected.action.select.serie
};
